# Database
DATABASE_URL=postgresql+psycopg2://radiancefleet:radiancefleet@localhost:5432/radiancefleet

# Config file paths (relative to backend/ directory)
CORRIDORS_CONFIG=../config/corridors.yaml
RISK_SCORING_CONFIG=../config/risk_scoring.yaml

# Logging
LOG_LEVEL=INFO

# Gap detection thresholds
GAP_MIN_HOURS=2.0
GAP_ALERT_HOURS=6.0

# STS detection
STS_PROXIMITY_METERS=200.0
STS_MIN_WINDOWS=8

# Optional: Commercial satellite API keys (never commit real values)
# CAPELLA_API_KEY=
# PLANET_API_KEY=
# UMBRA_API_KEY=

# Optional: External data APIs
# OPENSANCTIONS_API_KEY=

# Data fetcher
# DATA_DIR=data
# DATA_FETCH_TIMEOUT=120.0

# aisstream.io — Real-time AIS via WebSocket (free, requires sign-up)
# Get your API key at: https://aisstream.io/
# AISSTREAM_API_KEY=your_key_here
# AISSTREAM_WS_URL=wss://stream.aisstream.io/v0/stream
# AISSTREAM_DEFAULT_DURATION=300

# Global Fishing Watch — Vessel events + SAR detections (free non-commercial)
# Get your token at: https://globalfishingwatch.org/our-apis/
# GFW_API_TOKEN=your_token_here

# Copernicus CDSE — Sentinel-1 SAR catalog (free, requires registration)
# Register at: https://dataspace.copernicus.eu/
# COPERNICUS_CLIENT_ID=your_client_id
# COPERNICUS_CLIENT_SECRET=your_client_secret

# AISHub — Batch AIS positions (free membership)
# Join at: https://www.aishub.net/
# AISHUB_USERNAME=your_username
