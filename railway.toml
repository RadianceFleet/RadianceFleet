# Railway deployment configuration (multi-service — start commands set per service)
# See: https://docs.railway.com/reference/config-as-code

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
